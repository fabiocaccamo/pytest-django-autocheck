"""Test suite for pytest-django-autocheck.

These tests exercise the plugin itself (dogfooding) against the minimal Django
project defined under ``tests/`` (settings, urls and the ``exampleapp`` app).
"""
